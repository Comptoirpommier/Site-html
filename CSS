body {
    font-family: Verdana, sans-serif;
    margin: 0;
    padding: 0;
    text-align: center;
    background-color: #f9f9f9;
}

.logo {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 10%; /* Taille du logo en pourcentage de la largeur de l'écran */
    max-width: 100px; /* Limite la largeur du logo à 150 pixels */
    opacity: 0.5; /* Opacité du logo */
}

header {
    background-color: #ffffff;
    padding: 20px;
    position: relative;
}

h1 {
    color: #000;
    font-size: 2em;
}

.contact-info p, .contact-info a {
    color: #007bff;
    font-size: 1.2em;
    margin: 5px 0;
}

.contact-info a {
    text-decoration: none;
}

nav ul {
   list-style: none;
    padding: 0;
}
}

nav ul li {
    display: inline-block;
    margin: 0 15px;
    padding: 10px 0;
}

nav ul li a {
    text-decoration: none;
    color: #007bff;
    font-size: 1.5em;
}
/* Styles pour le pied de page et les icônes sociales */
footer {
    background-color: #ffffff;
    padding: 10px;
    position: relative;
    bottom: 0;
    width: 100%;
}

.social-links {
    margin-top: 30px;
}

.social-links img {
    width:10px; /* Ajustez la taille selon vos besoins */
    margin-right: 60px; /* Ajoutez une marge entre les icônes */
}

/* Réglages pour les appareils mobiles */
@media screen and (max-width: 600px) {
    .logo {
        width: 50%; /* Réduire la taille du logo pour les petits écrans */
    }

    h1 {
        font-size: 1.5em; /* Réduire la taille du titre pour les petits écrans */
    }

    nav ul li {
        display: block;
        margin: 10px 0;
        padding: 15px 0;
    }

    nav ul li a {
        font-size: 1.2em; /* Réduire la taille des liens de navigation pour les petits écrans */
    }
 .social-links img {
        width: 10px; /* Réduire la taille des icônes pour les petits écrans */
    }
}
