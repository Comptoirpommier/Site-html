body, html {
    font-family: Verdana, sans-serif;
    height: 100%;    
    margin: 0;
    padding: 0;
    text-align: center;
    background-color: #f9f9f9;
}

.logo-wrapper {
    margin-bottom: 20px;
}

.logo {
    width: 10%; /* Taille du logo en pourcentage de la largeur de l'écran */
    max-width: 20px; /* Limite la largeur du logo à 100 pixels */
    opacity: 0.5; /* Opacité du logo */
}

header {
    background-color: #b76e79;
    padding: 20px;
    width: 80%;
    display: flex;
    flex-direction: column;
    justify-content: center; /* Centre verticalement */
    align-items: center; /* Centre horizontalement */
    height: 100%; /* Utilise toute la hauteur de la fenêtre */
}

h1 {
    color: #000;
    font-size: 2em;
}

.contact-info p,
.contact-info a {
    color: #007bff;
    font-size: 1.2em;
    margin: 5px 0;
}

.contact-info a {
    text-decoration: none;
}

nav ul {
    list-style: none;
    padding: 0;
    display: flex;
    flex-direction: column; /* Change l'orientation des items à verticale */
    align-items: center;
}

nav ul li {
    margin: 10px 0;
}

nav ul li a {
    text-decoration: none;
    color: #007bff;
    font-size: 1.5em;
}

/* Styles pour le pied de page */
footer {
    background-color: #ffffff;
    padding: 10px;
    width: 100%;
    display: flex;
    justify-content: center;
    margin-top: 20px;
}

.social-links {
    margin-top: 30px;
    display: flex;
    justify-content: center;
}

.social-links img {
    width: 20px; /* Ajustez la taille des icônes selon vos besoins */
    height: auto;
    margin-right: 10px; /* Ajoutez une marge entre les icônes */
}

/* Réglages pour les appareils mobiles */
@media screen and (max-width: 600px) {
    .logo {
        width: 50%; /* Réduire la taille du logo pour les petits écrans */
    }

    h1 {
        font-size: 1.5em; /* Réduire la taille du titre pour les petits écrans */
    }

    nav ul li {
        display: block;
        margin: 10px 0;
    }

    nav ul li a {
        font-size: 1.2em; /* Réduire la taille des liens de navigation pour les petits écrans */
    }

    .social-links img {
        width: 10px; /* Réduire la taille des icônes pour les petits écrans */
    }
}
